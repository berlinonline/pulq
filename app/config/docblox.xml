<?xml version="1.0" encoding="utf-8"?>
<docblox>

    <title>Midas - your content broker of choice</title>

    <parser>
        <default-package-name>midas</default-package-name>
        <target>etc/integration/build/api/serverside</target>
        <markers>
            <item>TODO</item>
            <item>FIXME</item>
        </markers>
    </parser>

    <transformer>
        <target>etc/integration/build/api/serverside</target>
    </transformer>

    <logging>
        <level>warn</level>
        <paths>
            <default>logs/{DATE}.log</default>
            <errors>logs/{DATE}.errors.log</errors>
        </paths>
    </logging>

    <files>
        <directory>app</directory>
        <ignore>*cache*</ignore>
        <ignore>*config*</ignore>
        <ignore>*templates*</ignore>
    </files>
</docblox>
